var openshift = require('./lib/openshift');
var openshift_app = openshift({hostname: 'https://nodefly-rjdemo.rhcloud.com'});
//var openshift_app = openshift();
console.log(openshift_app.ip);
console.log(openshift_app.port);
console.log(openshift_app.hostname);
openshift_app.refresh_groups(function(result){console.log(result)});
